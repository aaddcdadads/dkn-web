stages:
  - install
  - publish

cache:
  untracked: true
  key: "jeecgboot-vue3"
  paths:
    - node_modules/

install:
  stage: install
  tags:
    - shared
  only:
    - master
  script:
    - tyn

publish:
  stage: publish
  tags:
    - shared
  only:
    - master
  script:
    - bash ./deploy.sh
