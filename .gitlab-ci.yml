stages:
  - install
  - publish

cache:
  untracked: true
  key: $CI_COMMIT_REF_SLUG
  paths:
    - node_modules/

install:
  stage: install
  tags:
    - sz-server3
  only:
    - master
  script:
    - tyn

publish:
  stage: publish
  tags:
    - sz-server3
  only:
    - master
  cache:
    paths:
      - node_modules/
    policy: pull
  script:
    - bash ./deploy.sh
